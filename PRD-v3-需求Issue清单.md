# CaseFile PRD v3 — 需求 Issue 清单（供 Copilot 批量创建 GitHub Issues）

共 38 个 Issue（P0：24 个，P1：14 个）。

## 给 Copilot 的使用说明

把本文件交给 Copilot（IDE 或 CLI），指示：

> 读取 PRD-v3-需求Issue清单.md，为每个 `---` 分隔的 Issue 块调用 `gh issue create` 创建一个 GitHub Issue，标题、标签、milestone、正文照抄；无 milestone 参数时跳过。创建前先确认 labels 与 milestones 已存在。

## 预先创建的 Labels

- 需求
- P0
- P1
- 模块-建案中心
- 模块-候选稿
- 模块-工作台
- 模块-会话设置

## 预先创建的 Milestones

- v3 首发（P0）
- v3 后续（P1）

## 打勾规则（状态流转）

- 开发者完成开发：在 Issue 验收标准处逐条勾选自测结果，把 Issue 移入「待验收」看板列；不改 PRD；

- 验收人逐条核对验收标准，全部通过后关闭 Issue（= 打勾 🟢），并在 Issue 留言记录验收结论；

- 每周由验收人批量同步 PRD v3 的状态标记（🟡 → 🟢）。

- S2 条目（无验收标准）暂不建 Issue，进入 S1 拆分后另行创建。

---
title: "R-101 起案原文输入"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
把灵感原样记录为不可替换的原始来源，后续生成与审阅都以它为锚点。

## 关键行为
1. 提供原文输入框（上限 2000 字）、清空、示例范文载入；
2. 原文未输入时阻断进入下一步并提示；
3. 原文持久化到服务端项目来源记录，修改产生新 revision；采用润色稿时保留父来源记录关联；
4. 后续步骤的实时简报映射持续显示当前概念与来源标记。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 输入超过 2000 字符被阻止且计数正确；
- [ ] 空原文点击"继续关键追问"出现阻断提示，不改变步骤；
- [ ] 服务端存在 source_record，内容与输入一致；修改后 revision 递增，历史可追溯。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-101）

---
title: "R-102 润色校样"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
让 Agent 提供三种力度的表达整理，但任何整理都必须先形成独立校样，由用户逐字确认，绝不直接改写原文。

## 关键行为
1. 三档模式：轻度校对（只处理错字病句标点）/ 表达优化（改善措辞语序节奏）/ 叙事增强（增强画面感，新增细节单独披露）；
2. 生成结果以"原稿对校"双栏呈现：原文只读，校样可编辑，标注 Agent 建议来源；
3. 显示修改说明与"新增细节审阅"清单；
4. 用户可保留原文或采用校样；采用后新稿成为当前来源，旧版仍可追溯。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 三档模式下原文内容在任何时刻不被直接修改；
- [ ] 叙事增强档若有新增细节，必须出现在"新增细节审阅"区；
- [ ] 采用后 source_record 的父记录指向原稿；保留原文则不产生新来源。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-102）

---
title: "R-103 关键追问"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
只追问会显著改变创作方向的关键信息，不把建案变成冗长问卷。

## 关键行为
1. 首轮最多 2 问，其中硬问题（必答）最多 1 道；每题展示问题、影响说明、建议选项（快速采用）与必答/可暂缓标记；
2. 可选问题可标记"稍后决定"，进入待决定队列且不阻断流程；
3. 可再生成补充问题：保留已有问题与回答，最多补充 2 道不重复的可选问题；
4. Agent 判断无需追问时允许直接形成简报。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 首轮问题数 ≤ 2 且必答问题数 ≤ 1；
- [ ] 补充生成后，已有回答内容不变、新旧问题无重复；
- [ ] 全部必答问题未回答时，"形成创作简报"被阻断并提示。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-103）

---
title: "R-104 创作简报生成"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
把原稿与已确认回答汇总为结构化、可逐项审阅的创作简报候选；也允许用户完全手动建立简报。

## 关键行为
1. 必答问题全部回答后才能触发 Agent 汇总生成；
2. 生成中展示任务进度，失败时回退到追问步骤并提示；
3. "手动建立简报"生成不含任何伪造 Agent 结果的人工简报；
4. 每个字段携带来源标识：你的原文 / 由你确认 / Agent 建议 / 尚未决定。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 必答问题未全答时无法触发生成；
- [ ] Agent 生成的字段来源标识正确区分建议与确认；
- [ ] 手动简报的所有字段来源为"你的原文/由你确认"，无"Agent 建议"。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-104）

---
title: "R-105 Brief 表单编辑"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
让用户按表单逐项校核与修改简报，修改立即标记为用户确认。

## 关键行为
1. 字段：一句话概念（必填）、推理目标（必填）、结论处理方式（按作者底牌展开 / Agent 提出候选结论 / 保持开放）、作者底牌（仅底牌模式必填）、核心卖点（列表编辑 + 拖动排序）、内容骨架（分阶段列表）、预计规模、风险提示；
2. 字段修改后来源标记切换为"由你确认"；
3. 缺必填项时保存/进入审阅被阻断并列出缺项。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 修改任意字段后该字段来源变为"由你确认"；
- [ ] 必填项缺失时"进入创作简报审阅"被阻断并逐项列出；
- [ ] 结论处理方式切换为"保持开放"时作者底牌字段清空且不再要求必填。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-105）

---
title: "R-106 约束抽屉"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
在建案阶段收集创作边界，形成后续生成必须遵守的约束集。

## 关键行为
1. 六类约束槽：必须保留、禁止出现、规模、人数、时长、内容尺度；
2. 每条约束可填语句并可切换硬约束/软偏好；
3. 已填写数量在抽屉入口汇总显示。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 六类约束槽全部可编辑，强度可切换；
- [ ] 约束语句进入简报候选并被后续候选生成引用（约束摘录可见）。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-106）

---
title: "R-107 对话修改与候选版本"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
通过一轮自然语言指令从当前候选生成子版本，任何修改都不覆盖旧版本。

## 关键行为
1. 提交修改指令后，从当前候选生成新的独立候选（origin=dialogue）；
2. 候选历史区展示全部候选：来源标注（Agent 初稿 / 表单修改 / 对话修改）、创建时间、当前/已保存/历史标记；
3. 支持保存为新候选、书签、恢复此版（激活）。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 生成子版本后原候选内容不变；
- [ ] 恢复历史候选后当前编辑区内容切换为该候选，且其余候选保留。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-107）

---
title: "R-108 创作简报审阅与原子拆解"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
把"生成依据"钉成纸面：核心字段 + 原子底牌 + 原子创作约束，逐条可确认、可人工增补。

## 关键行为
1. 审阅单字段：创作意图、核心推理命题、结论处理方式、作者底牌原文（仅底牌模式）、创作边界原文（每行一项）；
2. Agent 将底牌原文拆解为原子锚点、将边界原文拆解为原子约束（硬/软），来源标注 Agent 拆解；
3. 原文修改后旧拆解失效，提示重新拆解；支持人工新增原子项；
4. 门禁顺序：原子拆解完整 → 保存审阅 → 确认并冻结。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 修改作者底牌或边界原文后，对应原子列表清空并提示失效；
- [ ] 原子项未确认或审阅未保存时"确认并冻结"不可用；
- [ ] 重新拆解（Agent 任务）后原子项更新，原有已确认项以新结果为准。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-108）

---
title: "R-109 Brief 冻结与版本"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
冻结产生不可变的生成依据版本，任何后续生成都锁定该版本。

## 关键行为
1. 冻结后生成版本号（version_no），后续候选生成锁定该版本；
2. 修订流程：从候选页发起 → 返回简报成案 → 版本号 +1 → 重新审阅冻结；旧简报候选归档为 stale；
3. 服务端确认接口（confirmBrief）校验冻结门禁。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 冻结后工作版本号 = 冻结版本号；
- [ ] 修订后冻结状态清空，新版本重新生成候选时旧候选标记为旧简报；
- [ ] 门禁不满足（原子项缺失/未保存）时冻结请求被拒绝。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-109）

---
title: "R-110 建案完成度映射"
labels: ["需求", "模块-建案中心", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
让用户随时看到建案进展与遗漏，知道"进行到哪、还差什么"。

## 关键行为
1. 五项信号：起案原文、关键问题、创作简报、审阅冻结、当前工作稿；
2. 实时显示当前概念与来源标记；待决定队列提示。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 五项信号随步骤推进逐一点亮，计数正确；
- [ ] 存在待决定项时侧栏显示"待决定 1"及说明。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-110）

---
title: "R-111 建案入口选择（四条路径）"
labels: ["需求", "模块-建案中心", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
四条建案路径并列展示，用户无需理解内部结构即可选择起点。

## 关键行为
1. 四条路径并列展示，各含名称与摘要：A 我有一个想法 / B 帮我想一个 / C 我有已有内容 / D 我已经准备好；
2. 当前仅 A 可用；B/C/D 标记"后续开放"且禁用（R-112~R-114 完成后逐个启用）；
3. 路径选择影响后续建案流程入口，不丢失已输入内容。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 四条路径全部可见，仅 A 可进入；
- [ ] B/C/D 禁用且有"后续开放"说明；
- [ ] 入口区域标注当前路径。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-111）

---
title: "R-112 建案路径 B：帮我想一个"
labels: ["需求", "模块-建案中心", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
用户不输入内容即可获得多个差异明确的创意方向，比较后选择其一继续建案。

## 关键行为
1. 生成多个创意候选：一句话概念、核心悬念或任务、推理类型与结论模式、目标体验、主要设计风险、预计规模；
2. 支持并排比较、收藏、淘汰、重新生成单个候选；
3. 用户选择候选后才生成 Brief。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 候选之间差异明确（非仅改名或换背景）；
- [ ] 未选择的候选可保留为灵感资产，但不得写入 CaseFile；
- [ ] 选择候选后才进入简报生成。

## 状态
- 当前状态：🚧 待开发
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-112）

---
title: "R-113 建案路径 C：已有内容反向解析"
labels: ["需求", "模块-建案中心", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
把已有剧本或任务文档结构化，抽取实体、事件、信息与知识状态，并定位矛盾、缺失和知识泄露。

## 关键行为
1. 导入纯文本、Markdown、DOCX 与文本型 PDF；
2. 反向解析：核心实体与别名、事件及顺序、信息单元及来源、人物知识状态、关系与因果、候选目标问题与结论；
3. 结果分级：已明确 / 高可信推断 / 待用户确认 / 前后冲突 / 缺失但可能重要；
4. 逐项确认后形成 Brief 与 Draft。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 每个抽取项能跳转到来源片段；
- [ ] 系统不得将推断伪装成用户原文事实；
- [ ] 未经确认的高风险项不得进入 Canon；
- [ ] 解析失败时保留原文件和已完成结果。

## 状态
- 当前状态：🚧 待开发
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-113）
- **明确不做**：图片扫描件、音频、视频解析。

---
title: "R-114 建案路径 D：模板与空白工作台"
labels: ["需求", "模块-建案中心", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
为"已准备好"的用户提供模板或空白结构直接开始。

## 关键行为
1. 模板选择或空白结构开始；
2. 直接进入对应结构（跳过或轻量确认 Brief）。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 模板选择后进入对应结构，不产生伪造的 Agent 结果；
- [ ] 无模板时提供空白工作台入口。

## 状态
- 当前状态：🚧 待开发
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.1 建案中心（R-114）
- **依赖**：R-317 工作台真实数据接入。

---
title: "R-201 策略分析与深稿生成"
labels: ["需求", "模块-候选稿", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
基于同一份冻结简报，先由真实 Agent 生成三种差异明确的创作策略，再由用户选择一种生成一份完整深稿，并完成结构与引用校验。

## 关键行为
1. 前提：简报已冻结、当前版本未生成过候选、无进行中任务；
2. 策略分析任务一次返回三种方向，展示焦点、擅长项、需要接受和 Brief 适配理由；
3. 用户选择策略后才启动一份分区深稿生成，阶段展示解析冻结简报 → 生成分区 → 校验对象与引用；
4. 供应商认证失败时按配置顺序回退；任一任务失败时不归档不完整结果，状态回到可重试，不覆盖已有候选。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 策略分析成功时恰好返回三种差异明确的策略，且均关联当前冻结版本；
- [ ] 未选择策略时不能启动深稿生成，选择后成功归档一份通过结构与引用校验的深稿；
- [ ] 失败后状态回到可重试，已归档候选不受影响；旧简报修订后的候选与当前版本候选可同时存在并正确区分。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.2 Draft 候选与工作稿（R-201）

---
title: "R-202 策略比较、深稿预览与采用"
labels: ["需求", "模块-候选稿", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
让三种创作策略同时摊开，用户选择一种生成深稿，预览后显式采用为当前工作稿。

## 关键行为
1. 策略卡片展示：焦点（结构/氛围/推理）、方向、Brief 适配理由、擅长项和需要接受；
2. 选择策略后展示对应深稿卡片，包含标题、摘要、核心推理命题、对象计数和约束摘录；
3. 支持预览工作台并载入该深稿；采用仅限当前冻结版本候选，旧简报候选只读。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 三种策略方向、焦点和代价互不相同（非仅改名）；
- [ ] 选择策略后只生成对应深稿，采用后标记"当前工作稿"，再次采用被禁用；
- [ ] 旧简报候选的采用按钮禁用并说明原因。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.2 Draft 候选与工作稿（R-202）

---
title: "R-203 工作稿状态机与简报修订"
labels: ["需求", "模块-候选稿", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
管理"待采用 → 当前 → 旧简报"的工作稿生命周期，并支持回到简报修订。

## 关键行为
1. 状态判定：当前采用 = current；同版本未采用 = pending；旧简报 = stale；
2. "建立简报修订"返回简报成案步骤开始新版本；
3. 重置会话回到未建案状态。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 修订后旧候选状态 = stale 且不可采用；
- [ ] 重置后全部建案状态清空，后续操作创建新项目。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.2 Draft 候选与工作稿（R-203）

---
title: "R-301 对象树、筛选搜索与详情联动"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
按卷宗对象组织核对入口，支持快速定位。

## 关键行为
1. 五类对象：人物、证据、事件、地点、假设；按类型筛选（互斥）与关键词搜索；
2. 选中对象在画布/检查器联动显示详情与关系；
3. 对象详情包含内容、元信息与关联事件。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 五类对象均可浏览，筛选互斥且计数正确；
- [ ] 搜索可按名称/编号命中对象；
- [ ] 选中对象后检查器显示对应详情。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-301）

---
title: "R-302 视图体系（8 种）"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
以多种视角核对同一卷宗。

## 关键行为
1. 八种视图：时间线、关系图、推理图、地图、卷宗、导出、编译中心、证据对比；
2. 视图切换后对象选择保持联动。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 八种视图均可进入且切换后选中对象联动；
- [ ] 各视图数据源自同一工作稿，不互相漂移。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-302）

---
title: "R-303 画布交互"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
画布支持基础的图操作，便于核对与讲解。

## 关键行为
1. 节点拖拽、画布平移、缩放控制；
2. 工具切换（选择 / 平移）；图中每条边有来源对象，悬停可定位。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 拖拽、平移、缩放操作流畅且状态可重置；
- [ ] 悬停边可定位到来源对象。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-303）

---
title: "R-304 推理视图与论证图"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
让"为什么能得出这个结论"可检查。

## 关键行为
1. 列式推理路径：证据 → 步骤 → 结论，假设三态（supported / contested / eliminated）；
2. 论证图扩展：支持边（claim→evidence）、推导边（claim→claim）、反驳边（claim→claim）、竞争边（hypothesis→hypothesis）、规则违反红边（rule_violation）；
3. 选中推理节点后展开来源证据、验证日志与影响范围。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 每条推理路径的步骤与证据可展开核对；
- [ ] 论证图扩展完成后，边类型与前端展示、后端验证共用同一表示。

## 状态
- 当前状态：🟡 demo 已验证（推理图雏形）；论证图扩展 🚧 待开发
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-304）

---
title: "R-305 时间线三视图"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
区分真实发生顺序、用户暴露顺序与冲突视图，定位时间线错位。

## 关键行为
1. 法证视图：按真实发生顺序排列事件；
2. 叙事视图：按用户/玩家体验到的暴露顺序排列；
3. 矛盾视图：标出时间冲突与待验证事件。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 三种视图切换后同一事件的时间标签正确区分；
- [ ] 矛盾视图仅列出存在冲突或待验证的事件；
- [ ] 事件在法证/叙事视图中的排序可独立调整。

## 状态
- 当前状态：🚧 待开发
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-305）

---
title: "R-306 地图视图与标记联动"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
按地点核对事件与对象分布。

## 关键行为

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 地图标记与事件/对象选择双向联动；
- [ ] 地图可平移，标记可点击定位。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-306）

---
title: "R-307 验证问题、引用来源与补丁审阅"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
每个问题可定位到对象与证据，并支持补丁式处理。

## 关键行为
1. 验证问题四状态：open / patch-ready / resolved / exception；分级 S0/S1；
2. 检查器展示问题、关联证据（引用来源）、影响对象；
3. 问题标记"补丁待审批"进入补丁审阅 tab，可批准解决或豁免。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 未解决问题数统计正确；
- [ ] 批准补丁后问题状态变为 resolved；豁免进入 exception 且可追溯。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-307）

---
title: "R-308 验证运行与审计记录"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
验证可重新运行，所有动作有审计痕迹。

## 关键行为
1. 重新计算 / 运行验证两种触发，运行中显示阶段状态；
2. 审计记录 tab 按时间列出动作、执行者、操作与详情。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 运行中 UI 明确标记 busy，完成或失败后状态恢复；
- [ ] 每次补丁操作写入审计记录。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-308）

---
title: "R-309 证据抽屉与音频播放器"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
集中查看证词、转写、模型日志与检索命中，辅助核对。

## 关键行为
1. 四个 tab：证词录音、转写文本、模型日志摘要、检索命中，带计数；
2. 证词录音支持播放/暂停与进度（播放头）；
3. 抽屉内材料与当前选中对象/问题联动。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 四个 tab 内容可切换且计数正确；
- [ ] 音频可播放/暂停且进度显示正确；
- [ ] 切换选中对象后抽屉内容联动更新。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-309）

---
title: "R-310 命令面板与键盘快捷键"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
键盘优先的快速操作，减少跳页。

## 关键行为
1. 命令面板：切换视图、打开问题、打开录音等命令，支持关键词过滤；
2. 快捷键：时间线键盘导航、对话框 Esc 关闭与焦点陷阱。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 面板命令可用关键词过滤并执行；
- [ ] 焦点陷阱对话框 Tab/Esc 行为正确，不逃逸焦点。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-310）

---
title: "R-311 候选接力条"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
在工作台内快速切换候选工作稿，明确当前版本。

## 关键行为

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 接力条状态与候选状态机（R-203）一致；
- [ ] 切换候选后画布与检查器联动更新。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-311）

---
title: "R-312 案件概览页"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
提供"总卷宗封面"，一眼看清案件状态与关键风险。

## 关键行为
1. 头部：标题、状态、版本、负责人、风险等级；
2. 中央：案件摘要、真相摘要、题材标签、目标模板；
3. 右上：生成进度、验证状态、成本用量；
4. 下半区：关键嫌疑人、关键证据、关键冲突、最近改动。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 概览信息与工作稿数据一致；
- [ ] 各区块可跳转到对应工作台视图。

## 状态
- 当前状态：🚧 待开发
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-312）

---
title: "R-313 布局调整与移动端切换"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
四区域布局可调节，窄屏下可切换区域。

## 关键行为
1. 左栏/检查器/时间线宽度拖拽调整；
2. 移动端四区域（对象 / 主画布 / 检查器 / 来源）切换。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 拖拽调整后布局保持且不溢出；
- [ ] 窄屏切换区域后状态保持。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-313）

---
title: "R-314 导出与卷宗视图"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
以卷宗档案与导出两种形态查看与交付内容。

## 关键行为
1. 卷宗视图：对象档案清单式浏览；
2. 导出视图：选择导出范围与格式，展示导出结果。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 卷宗视图对象与对象树一致；
- [ ] 导出视图可选择范围并产出结果文件。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-314）

---
title: "R-315 编译中心视图（样例）"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
演示"同一卷宗 → 多种形态"的编译概念与发布门禁，**正式 Compiler 仍为 S3**。

## 关键行为
1. 多种目标格式选择（如小说、剧本、互动、卷宗、测试），展示编译预览；
2. 发布门禁展示：结构完整性、引用可追溯、语义验证（未解决问题阻断）；
3. 明确标注产物为"开发样例，正式版本由 Compiler 生成"。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 目标格式可切换且预览随选择更新；
- [ ] 存在未解决问题时发布门禁显示阻断；
- [ ] 页面不产生可交付的正式编译产物（避免与 S3 编译器混淆）。

## 状态
- 当前状态：🟡 demo 已验证（本地样例数据）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-315）

---
title: "R-316 工作台对话"
labels: ["需求", "模块-工作台", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
在工作台内以对话方式向 Agent 提问或发起局部修改。

## 关键行为
1. 对话面板 UI 已具备（消息、思考状态、意图回复），当前为本地模拟；
2. 后端接线：基于当前选中对象与工作稿上下文发起对话（casefile-chat agent 已存在）；
3. 对话产生的修改以候选/补丁形式呈现，不直接写入工作稿。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 接线完成后，对话上下文携带当前对象引用；
- [ ] 对话结果不改变工作稿事实，除非用户显式批准。

## 状态
- 当前状态：🟡 demo 已验证（对话 UI，本地模拟）；后端接线 🚧 待开发
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-316）

---
title: "R-317 工作台接入服务端 CaseFile 数据"
labels: ["需求", "模块-工作台", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
工作台从本地样例数据切换为服务端 CaseFile 文档真实数据。

## 关键行为
1. 采用候选后，工作台载入该候选对应的服务端 casefile 文档（对象、事件、信息、推理路径、验证问题）；
2. 对象编辑走 v1 对象补丁接口并带 revision 冲突检测；
3. 样例数据仅保留为开发态降级（无后端时），正式态默认真实数据；
4. 快照（snapshot）与验证结果与文档版本绑定。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 工作台展示的实体/事件/信息数量与候选对象计数一致；
- [ ] 编辑对象后服务端 revision 递增，并发冲突时给出提示；
- [ ] 无后端会话下显示"本地样例"降级标识。

## 状态
- 当前状态：🚧 待开发（S1 最大正式化缺口）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.3 分析师工作台（R-317）

---
title: "R-401 项目与会话生命周期"
labels: ["需求", "模块-会话设置", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
一次建案会话对应一个服务端项目，来源与简报数据可持久化。

## 关键行为
1. 首次保存原文时自动创建项目；
2. 来源、问题回答、简报候选、审阅、冻结与候选采用均持久化；
3. 重置会话清空前端状态并允许创建新项目。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 使用 `/?project=<id>` 刷新或重进后，关键状态可从服务端恢复并定位到正确步骤；
- [ ] 服务端项目与来源、简报、任务数据一致。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.4 会话、任务与设置（R-401）

---
title: "R-402 长任务执行与失败处理"
labels: ["需求", "模块-会话设置", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
所有 Agent 任务（润色、追问、简报生成、拆解、候选生成）通过统一任务机制执行，失败可恢复。

## 关键行为
1. 任务创建（202）→ 轮询等待 → 读取结果；任务事件可查；
2. 回退重试前重取最新 revision（任务创建会推进 revision）；
3. 模型供应商回退：首个任务验证可用 provider 后复用；
4. 重进项目时检测 queued/running/cancelling 任务并继续轮询；
5. 失败时前端回到可重试状态并给出原因，不破坏已归档数据。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 任意 Agent 任务失败后 UI 回到可重试状态，提示明确；
- [ ] 同一会话后续任务可继续完成；
- [ ] provider 不可用时有明确错误而非静默挂起。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.4 会话、任务与设置（R-402）

---
title: "R-403 模型服务设置"
labels: ["需求", "模块-会话设置", "P0"]
milestone: "v3 首发（P0）"
---
## 目标
用户可配置模型供应商、密钥与模型，并查看凭据验证状态。

## 关键行为
1. 供应商选择（OpenAI / DeepSeek）、API Key 输入（可显隐）、模型选择；
2. 凭据状态展示：待验证 / 已验证 / 可用 / 不可用；
3. 设置入口：顶栏账户按钮，全局事件触发。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 保存后凭据状态正确更新（不可用时有明确提示）；
- [ ] 设置对话框可在建案中心与工作台顶部打开。

## 状态
- 当前状态：🟡 demo 已验证
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.4 会话、任务与设置（R-403）

---
title: "R-404 产品级 Benchmark（评测与回归）"
labels: ["需求", "模块-会话设置", "P1"]
milestone: "v3 后续（P1）"
---
## 目标
审批修改（PatchCandidate 审阅）、冻结版本并发布。

## 关键行为
1. 双层结构：产品流程 benchmark（生成 → 验证 → 编辑 → 转换 → 发布全链路）为主；子能力 benchmark（MuSR / LogiQA / ProofWriter / CLUTRR 等学术集）作为"能力雷达"，不进发布门槛；
2. 指标组：结构有效性（schema pass rate）、事实一致性（support coverage / unsupported rate）、逻辑闭合度（contradiction count / closure score）、转换保真度（projection fidelity）、人工可用性（editor acceptance rate）；
3. 指标分级：gate（阻断发布）/ target（期望，不阻断）/ observation（仅观察）；
4. 运行模式：fake（离线可复现）/ live（真实模型），可重复运行并聚合结果。

## 验收标准（验收人逐条核对，全部通过后关闭本 Issue）
- [ ] 对标准案件回归集可运行并输出指标报告；
- [ ] 同一 fixture 与模式重复运行结果可复现；
- [ ] gate 指标不满足时明确标记阻断，不静默通过。### 10.5 S2 已界定模块S2 模块只写目标、关键行为、依赖与明确不做；**禁止字段级细节与验收标准**，进入 S1 时再补充。#### S2-01 质量中心

## 状态
- 当前状态：🟡 后端 runner 已实现（fake/live 双模式）
- 开发自测 → 待验收；验收人核对通过 → 关闭 Issue → 同步 PRD v3 标记 🟢。

## 关联
- PRD：docs/CaseFile-PRD-v3.md · 10.4 会话、任务与设置（R-404）
- **依赖**：R-317 真实数据、版本冻结机制、R-404 Benchmark 指标回流。
- **明确不做**：三方 Diff 发布门禁（属 S2-04 审阅发布）；模型 grader（动机可信度、剧透程度等灰区判断）；复杂规则引擎。
- **依赖**：质量中心问题分级、Canon 版本冻结。
- **明确不做**：真实用户测试分享链接、世界规则多轮模拟（S3）。
- **依赖**：R-402 任务机制。
- **明确不做**：任务档位与预算计费（S3）。
- **依赖**：R-317 真实数据、R-307 补丁审阅雏形。
- **明确不做**：Release Package 与编译器产物（S3）。
