# N4.5-07 开发交接：可审计 Shadow 运行时

更新时间：2026-09-05。文档性质：**实施结果记录；07 工程验收完成，B3 资格仍未通过**。

## 1. 接手目标与用户决定

用户已决定暂停 B3 评审优化，保持当前 v2，优先推进开发进度。本轮按桌面工程计划执行 **N4.5-07：ORM、Alembic、Worker 生命周期、API Shadow 开关与读模型**。

本轮首先完成工程闭环及 Fake/真实 PostgreSQL 集成验证。不要自行恢复已回退的实验、修改评审 Prompt、重跑 B3、构建新私有资格包或启动 N4.5-08 的完整 B4 正式验收。保持 v2 是实现选择，**不意味着 B3 已通过，也不豁免资格门槛**。

本文保留实施前的基线与资格证据，并在第 10 节记录本轮运行时接入和测试库迁移的实际结果。没有执行新的 Live Provider 调用。

## 2. 仓库基线与历史

| 项目 | 当前状态 |
|---|---|
| 工作目录 | `C:/Users/Lenovo/Desktop/CaseFile-narrative-compiler` |
| 分支 | `N4.5` |
| 07 实施起点 | `351e6c9f`，干净工作区；包含下述代码基线及实施前交接文档 |
| 接手代码基线 | `0e2e0e117648019d5c3b7e3873820a4f42f0a8fc`，`revert: 回退后续评审实验并恢复B3 v2基线` |
| 用户指定恢复点 | `75f3441bb07a859e06bb97e659133f75568856d7`，`revert: 恢复B3 v2活动基线` |
| 一致性证据 | 创建本文前，两者 Git tree 均为 `1e4300d84fa4060acd18a55b782ad05ae76b3cd2`，工作区干净 |
| 回退方式 | 向前 revert，保留实验提交历史和 Git 忽略的本地报告 |

两轮后续开发实验均未取得满足冻结条件的改进证据，已回退：

- 独立单稿评估后再双位置比较：实现 `bfb06dbc`，结果 `bf0962dd`。
- 节奏与重复识别的定向标准：实现 `53697436`，结果 `e8e4317a`。

这些提交只供未来查证。不要从历史实验分支接续 07，也不要把实验里的 Prompt 编号混同于 B3 正式 v2/v3 的整体实现版本。创建本文以后，仓库可能多出纯文档提交；上述 tree 一致性描述的是交接前代码基线。

## 3. 已完成和未完成的阶段

| 阶段 | 工程状态 | 验收状态与证据 |
|---|---|---|
| 04 Writer | 组件与公开开发基线已实现 | 不将 Fake 开发报告当作正式 Writer 资格；B1 正式指标在后续 B4 中判定 |
| **05 Rewrite** | **已完成**：最多两轮整场重写，每轮完整复审 | **B2 已通过**，正式 revision `14c55c90`，24/24 修复、24/24 保真，新增关键问题和失败均为 0 |
| **06 Quality/Polisher** | **已完成组件实现**：findings、润色、保真、双位置评审、精确回滚 | **B3 未通过**，当前活动 v2：整体 13/16（要求 ≥14）、镜像 11/16（要求 ≥15）、保真 21/24（要求 24）；协议/基础设施失败为 0 |
| **07 Shadow 运行时** | **工程验收完成** | Fake/真实 PostgreSQL 和完整门禁通过；实际命令与结果见第 10 节 |
| 08 完整 E2E | 未开展本阶段交付 | B3 未通过前不启动完整 B4，不标记 N4.5 `qualified=true` |

05 正式 attempt 为 `b2-live-v3-20260903T042158Z`，报告位于：

`backend/var/benchmark/prose-rewrite/qualification-v1/b2-live-v3-20260903T042158Z/report.json`

报告 hash：`0ed76e53491c034f8ef9521600cb27fa0021ae28bd1279c6bc43d9b439b5b5c5`。本会话已复验自哈希及 `qualified=true`；当前重写核心与资格 revision 的差别仅是导入排序，Supervisor 无行为差异。

06 当前 v2 正式 attempt 为 `b3-live-v2-20260903T071751Z`；qualification-v2 已消费，入口拒绝再次运行。详细状态见 [05 交接](n4.5-05-handoff.md) 与 [06 交接](n4.5-06-handoff.md)。本文没有重新核验全部 B0 历史资格；后续若需要 Live 前置证据，应读取既有报告，不根据阶段序号推定通过。

## 4. 必读资料与使用顺序

1. 仓库根 `AGENTS.md`，以及其中列出的架构、前后端职责、契约、迁移、数据一致性、质量与 Git 规范。
2. 桌面 [N4.5-工程化开发计划.md](C:/Users/Lenovo/Desktop/N4.5-工程化开发计划.md:93)：第 3 节明确列出 00–08，07 在第 104 行。
3. 仓库 [正文渲染架构](n4.5-prose-rendering-architecture.md)：持久化权威、Artifact key、串行状态机、预算、恢复和 Shadow 失败隔离。
4. [组件与 Judge 协议](n4.5-agent-judge-protocol.md)、[Benchmark 资格协议](n4.5-benchmark-qualification.md)。
5. 05/06 交接、当前 Prompt registry 和组件常量，确认实际已冻结行为。

桌面还存在较早的 `N4.5-Supervisor-Multi-Agent-Development-Plan.md`，它按 PR1–PR6 划分并含旧方案。**本次阶段编号以较新的《N4.5-工程化开发计划》为依据**；字段和当前行为按仓库 ADR、契约及已验收组件核对，不照搬旧计划中的局部 patch 或旧状态机。

## 5. 当前可复用实现

以下源码均在 `backend/src/casefile/` 下：

| 职责 | 现有入口 |
|---|---|
| Checklist、正文规范化、精确引文 | `domain/narrative_compiler/prose_checklist.py` |
| 质量前置条件、双位置采用规则 | `domain/narrative_compiler/prose_quality.py` |
| Writer | `agent_runtime/prose_writer.py`，`execute_prose_writer` |
| Semantic Council、Consensus、Arbiter | `agent_runtime/prose_judge.py` |
| 完整 Rewrite 与有限编排 | `agent_runtime/prose_rewriter.py`、`prose_rewrite_supervisor.py`，`execute_bounded_prose_rewrite` |
| Quality findings 与双位置比较 | `agent_runtime/prose_quality_critic.py` |
| Polisher 与保真/质量回滚 | `agent_runtime/prose_polisher.py`、`prose_polish_supervisor.py`，`execute_prose_polish_supervisor` |
| 编译应用服务与常量 | `application/compiler/service.py`、`constants.py` |
| 编译 API、输入输出类型 | `api/compiler.py`、`api/schemas.py`；结合实际调用追踪类型定义 |
| 编译 Worker 及完成边界 | `worker/executors/compiler.py`、`story_planner.py`、`completion.py` |
| 编译存储与 ORM | `data_postgres/compiler_repository.py`、`models/compiler.py` |
| Step/Call 审计与 Worker fencing | `data_postgres/models/agent_execution.py`、`worker/observability.py`、`worker/execution.py`、`worker/queue.py` |
| 公共正文/编译契约 | 根目录 `contracts/schemas/compiler/prose-rendering.schema.json`、`compiler.schema.json` |

当前已存在 `CompileArtifact`、`AgentStepRun`、`AgentModelCall`、`CompileRun`、`TaskRun`，以及 N4.4 编译执行和集成测试。不要新建一套并行的通用 Run/Call/Result 表。

当前活动版本：

- Writer：`prose-writer-v1`。
- Rewrite：`prose-rewriter-v3`，`prose-rewriter-json-object-v3`，硬性最多两轮。
- Quality findings：`prose-quality-critic-v1`；Pairwise：`prose-quality-pairwise-v1`。
- Polisher：`prose-polisher-v2`。其目标长度是质量指导；不要恢复已移除的软长度硬拒绝。
- 生成、Rewrite、语义评审与 Polisher 使用 `deepseek-v4-pro`；Quality 使用 `deepseek-v4-flash`。不引入其他模型。

## 6. 07 工程实施范围

### 6.1 冻结输入与 API

- 增加默认 `false` 的 `prose_renderer_shadow`，贯穿请求校验、输入冻结、持久化与读模型，不只停留在请求 DTO。
- 显式启用只面向 `preview`、DeepSeek、Profile v2，并要求可用且精确匹配的 ScenePlanIR v2/NarrativeIR。Profile v1 和原 N4.4 编译路径保持兼容。
- 前置不满足应在正文 Provider 调用前阻断。按现有冻结协议表达 Shadow 状态，不把 N4.5 失败改写成 N4.4 失败。
- 本阶段只做后端契约及必要的生成物同步，不新增前端页面。

### 6.2 数据库与不可变产物

- 核对现有 Artifact identity/check/unique 约束与仓库写入边界，再同步修改 ORM、迁移和数据库职责文档。沿用已有 Artifact kind。
- Checklist、各阶段 Render、各轮 Judge/Consensus/Arbiter、Preservation、Quality、NovelCandidate 和 CompileManifest 均保留哈希及来源链。
- 使用 ADR 的精确 key 规则；阶段固定为 `writer`、`rewrite_1`、`rewrite_2`、`polished`、`accepted`。同一 CompileRun 的 key 唯一，产物内容不可变。
- 双位置 Pairwise 的两份报告都须可审计，不能相互覆盖；对照现有 Schema/ADR 确认承载方式，不自行丢弃其中一次。
- 每个组件映射到 Step，每次真实调用映射到 Call；持久化原始响应、解析状态、用量、延迟、指纹，并保持作者侧读模型的公开边界。

### 6.3 Worker 串行编排

目标链路为：

```text
冻结的 N4.4 产物
  → Checklist → Writer → Semantic Review
  → 必要时 Rewrite 1/2，各自完整复审
  → Quality findings → Polisher → 完整 Preservation Council
  → 双位置 Quality → 最终接受原稿或润色稿
  → 下一场 → NovelCandidate / CompileManifest
```

- 按 `discourse_order` 串行；下一场只接收前一场最终接受全文及哈希。
- 先复用现有组件的请求、规范化、Evidence 与恢复校验，不重新实现评审规则。
- 语义拒绝、前置阻断或基础设施未决使 Shadow 后续场景停止，不能以未接受正文继续下一场。
- 润色语义失败或质量不稳定时精确回滚原稿，不将回滚视为整场语义拒绝。
- Shadow 默认关闭；不写 Draft、Canon、Exposure，不自动采用正文，不支持 canonical 正文生成。

### 6.4 恢复、取消与并发

- 沿用现有 TaskAttempt lease/fencing；在组件边界检查取消和所有权，旧 Worker 不得继续写入 Step、Call、Artifact 或终态。
- 只有完整 fingerprint 匹配且已持久化的结果才可复用；保留其真实用量和来源，不产生伪造的零成本调用。
- ScenePlan、NarrativeIR、Profile、前一场 accepted Render、Prompt、Schema、模型、policy 或上游产物变化必须使依赖结果失效。
- 覆盖重复 Worker、取消、lease 丢失、组件调用后崩溃、数据库写入失败和重领任务。区分已持久化结果可复用与外部调用结果未落库的未知窗口，不能用跳过审计宣称不重复计费。

## 7. 接手时必须对齐的两个细节

### Council 策略

桌面总图展示完整三角色能力，但当前 Writer/Rewrite 已冻结使用 `fidelity-only-v1`；润色后的 Preservation 使用完整三角色 Council，并按需调用一次 Arbiter。07 不应因为接入完整总图就悄悄更换前半链的策略。运行清单应显式记录每段实际 policy 及其 fingerprint。

### 预算与显式网络重试

07 按用户“不限制成本”的决定，将 `23/Scene` 明确为完整三角色流程的结构性逻辑调用上限，不设金额/物理请求成本闸门；较少角色的策略节省调用不能用于额外 Rewrite 或采样。当前 Judge 代码还允许对连接错误/超时做一次可审计的显式重试，SDK 隐藏重试仍关闭；Writer、Rewrite、Quality、Polisher 的网络重试为 0。

因此不能简单把“23 次预算”与“所有组件绝不重试”画等号。实施前先核对既有逻辑调用、物理 transport 和 Supervisor 扣预算方式，明确持久化计数与门禁口径，并与 ADR/组件协议同步。不要为了集成方便移除既有行为、遗漏物理调用或无声扩大上限；如发现文档与实现冲突，记录具体差异后处理。

## 8. 验收清单

- [x] Shadow 缺省关闭；关闭时既有编译行为保持兼容。
- [x] 合法启用与 Profile/provider/mode/输入不匹配的拒绝路径均覆盖。
- [x] 多场 Fake 完整链路通过，前一场接受稿绑定及下游失效正确。
- [x] 初稿通过、一次修复、两次修复及第二次仍失败停止均覆盖。
- [x] 润色成功、保真失败、质量 tie/位置分歧均产生正确接受或精确回滚。
- [x] 模型合法语义失败与协议/解析/Evidence/网络故障分开表达，不以聚合分数掩盖失败。
- [x] 所有 Step、Call、Artifact、内容哈希和来源链在真实 PostgreSQL 上可查证。
- [x] 重复 Worker、取消、lease fencing、恢复复用、写入失败与唯一键冲突均覆盖。
- [x] N4.5 Shadow 失败不破坏已成功的 N4.4 产物、原编译终态或 Draft。
- [x] Schema 生成、OpenAPI、迁移、ORM、后端职责图和数据库说明同步。
- [x] 完整 `scripts/check.ps1` 使用可丢弃 `_test` PostgreSQL 通过；`-SkipPostgres` 不能替代 07 运行时集成证据。
- [x] 如实报告“07 工程验收完成、B3 资格仍未通过”，不自动进入 08 或启用生产质量 Gate。

可复用的现有测试起点：`backend/tests/integration/test_narrative_compiler_runtime.py`，以及 `backend/tests/unit/test_prose_checklist.py`、`test_prose_writer.py`、`test_prose_rewriter.py`、`test_prose_judge_council.py`、`test_prose_quality_critic.py`、`test_prose_polisher.py`。

开发命令仍采用仓库入口：`scripts/new-migration.ps1` 创建迁移，`scripts/generate-contracts.ps1` / `scripts/check-contracts.ps1` 同步契约，`scripts/check.ps1` 运行质量门禁。数据库测试必须显式指向库名以 `_test` 结尾的可丢弃 PostgreSQL；不得对日常开发库执行破坏性测试。

## 9. 原实施任务（历史交接）

> 基于当前 N4.5 v2 代码基线，先阅读本交接与桌面《N4.5-工程化开发计划》的 07 条目。核对 API 输入冻结、Artifact 约束和 Worker 审计/恢复边界，完成 07 的工程实现与 Fake/PostgreSQL 集成验证。复用 04–06 现有组件，保持其活动模型与 Prompt，保留 Shadow 默认关闭。暂停 B3 能力优化，不运行新 Live Benchmark，不修改资格结论。先对齐当前 Council policy 及逻辑/物理调用预算，再落地接口、迁移、串行编排、读模型与生命周期测试。完成后更新本交接的实施结果及验证证据。

07 的建议工程提交标题沿用桌面计划：`feat: 接入N4.5可审计Shadow运行时`。上述是实施前交接建议；本轮已经按批准的 07 计划接入运行时，实施结果如下。


## 10. 07 实施结果与验证证据

### 已落地

- 公共 `prose_renderer_shadow=false` 默认值；启用必须为 preview/DeepSeek/Profile v2，自动选择 N4.4 ScenePlan v2 路径。旧 v1/关闭路径兼容，历史冻结 JSON/hash 不回写。
- 新迁移 `V20260904220930__prose_shadow_runtime.py`，沿用现有 Run/Artifact/Step/Call 表；扩展不可变 key/schema/payload identity 和逐物理请求恢复审计字段。
- 前半链保持 fidelity-only-v1；Preservation 保持完整三角色。Writer/Rewrite/Quality/Polisher 活动 Prompt 与模型未更换。
- Scene 串行、两轮完整 Rewrite、完整 Preservation、双位置独立报告、精确回滚、accepted/NovelCandidate/CompileManifest 全链落库。
- 完整 runtime/Prompt/公共及私有 Schema/Evidence policy/model/Scene 数/结构性调用上限冻结；逻辑调用与物理请求、未知 usage 分开计数。Judge 显式重试单独 Call，SDK 隐藏重试保持关闭。
- 同 Attempt/Worker/有效 lease fencing，包含正文启用后的 N4.4 回调；调用期间续租。已落库响应可重新解析，未落库结果标为未知并停止，不自动重发。
- 同 Worker 标识的新旧 Attempt 隔离；取消和过期租约取消都保留 Shadow 清单。数据库写入失败留待租约恢复，不能虚报成功。
- 主 N4.4 成功结果独立投影；TaskRun 保留执行/取消语义，Shadow 作为子结果。无 Draft/Canon/Exposure 写入，无自动采用。

### 验证记录

- 现有 Writer/Rewrite/Judge/Quality/Polisher 定向单元测试：81 passed。
- 新增 PostgreSQL 运行时测试 21 项全部通过，覆盖主链、回滚、协议故障、双位置身份、两轮停止、恢复、取消、fencing 和五类成功响应缺失 usage 的未知标记。
- 原 N4.4 与首批 N4.5 合并定向测试：28 passed；随后新增取消收敛、非零用量恢复、迁移兼容和 Arbiter/位置分歧证据。
- `scripts/check-contracts.ps1`：16 项 Python 契约/OpenAPI 测试通过，TypeScript 编译及往返通过，生成物零漂移。
- Ruff、mypy（326 source files）和架构检查通过。
- PostgreSQL 目标为本机 `casefile_test`（端口 55433）；没有迁移日常开发库，没有 Live Provider 调用。
- 最终完整门禁：**PASS，退出码 0，Repository checks passed.** 主 pytest：**1483 passed、9 skipped**，耗时 1806.12 秒；Phase 3：**2 passed**；Phase 4：**1 passed、3 skipped**。Phase 4 的跳过项属于其他 rollout 版本，脚本本次显式验证 v3；没有把跳过项计作通过。
- 日志 `backend/var/n45-07-check.log`（本机忽略文件，不入 Git），SHA-256：`3dc88a241ce169d90eb7881645187f587203e7a469901362d51924a4f7219a57`。

实际执行入口（数据库凭据不写入文档；执行前已显式设置 `CASEFILE_TEST_DATABASE_URL` 指向上述可丢弃库）：

```powershell
./scripts/generate-contracts.ps1
backend/.venv/Scripts/python.exe scripts/generate-openapi.py
./scripts/check-contracts.ps1
./scripts/check.ps1 *> backend/var/n45-07-check.log
```

首次完整测试发现新 JSONB 审计列未加入元数据测试清单，修复后重新执行完整门禁。另补充成功响应无 usage 的审计修复和测试，最终上述结果对应全部最终代码。日常开发库尚未迁移；使用新版后端前按既有迁移流程升级至 `20260904220930`。

### 资格与后续边界

07 工程验收不替代 B3/B4。B3 v2 的 Quality 13/16、镜像 11/16、Preservation 21/24 未变；仍未通过资格。不重跑已消费资格包，不创建新的私有包，不自动进入 08，不改变默认关闭。

## 11. 2026-09-05 开发诊断与阶段收尾

在后续用户授权下完成公开24对/24场、三次重复的Flash/Pro开发对照与原稿/润色保真诊断；随后对相同72份已通过Preservation的润色稿执行144次Pro Pairwise复评。详见 [公开诊断报告](n4.5-b3-diagnostic-report.md) 与 [Pro同稿复评报告](n4.5-b3-pro-rescore-report.md)。这些新增开发实验不改写前述07交付及历史正式资格证据。

Pro在公开偏好对上每轮整体21/24、镜像19/24，未满足镜像≥23/24；同稿润色采用从Flash的25/72提高至45/72，但三轮16/24、14/24、15/24仍未满足每轮≥18/24。没有晋级活动实现、创建新私有资格包或进入B4。

用户决定先提交并收尾，暂不安排人工审阅。当前阶段可按“工程完成、开发诊断结束、B3未通过”关闭；人工盲评与进一步模型优化列为未来可选工作，不视为本次交付欠项。保持活动v2和Shadow默认关闭；本阶段收尾不等于完整N4.5资格通过或上线授权。

验证证据：诊断版完整门禁1493 passed、9 skipped，Shadow PostgreSQL 21项通过；其后仅新增Pro复评benchmark入口与测试，相关回归37项通过，Ruff/mypy/架构检查通过。验证口径及日志hash按两份报告分别记录，不将较早的全量结果冒充后续版本的新全量运行。


## 12. 2026-09-05 合并 main 与 PR 验证

本地合并 main 的 `4fd2e51a`，重新生成合并后契约与 OpenAPI；Shadow 迁移直接前序调整为 `20260903224420`，不改写 main 已有迁移。同步消息上下文快照断言，公开泄漏检测仅在合法公共 DTO 的指定位置接受 `draft_revision`，其余私有字段和敏感值仍拒绝。

合并后 `scripts/check.ps1` 完整通过：1550 passed、9 skipped，含 21 项 Shadow PostgreSQL 回归；Phase 3 为 2 passed，Phase 4 为 1 passed、3 skipped。数据库显式使用可丢弃 `casefile_n45_merge_test`，未迁移日常开发库。日志 `backend/var/n45-main-merge-check.log` 的 SHA-256 为 `a1946da2b8460e5e37fdc52687a3c24583d82cbf750d8cd33f1316f6d34bb2c1`。契约检查 16 项及 TypeScript 往返通过；公开泄漏检测 11 项、Goal 交互 19 项回归通过。

完整后端检查期间 main 更新至 `119e2bac`，新增变更仅涉及前端工作台与前端职责文档，随后一并合入并重新执行前端完整检查。B3 未通过、活动 v2、Shadow 默认关闭等阶段结论不变。

最新前端完整检查通过：lint、类型检查、46 个测试文件的 456 项测试及生产构建全部通过。日志为 `backend/var/n45-latest-main-web-check.log`（本机忽略文件）。
