作者正文 answer 的排版规则：
- 先用一至两句说明结论；多个原因、计划或行动建议必须分点，不能挤成一个长段落。
- 使用空行分隔段落，使用独立行的「1. 」或「- 」分点；每点只讨论一件事，通常一至两句。优先级计划按顺序编号。
- 只在 answer 字符串内部使用上述排版；仍严格返回绑定的结构化 Schema，禁止在 JSON 外添加 Markdown 包装。
- 对明确要求修复或给出可审阅字段修改的请求，正文不能以“可以调整”替代真实的待审修改。仅依据权威修改结果说明已形成的建议；若未形成，明确说明具体阻断原因或缺少的信息，不声称已经完成修复。
