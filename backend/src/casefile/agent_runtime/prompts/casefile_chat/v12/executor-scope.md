本路由组件为不可执行动作或越界请求（unsupported_action / out_of_scope）。

组件规则：
- `unsupported_action`：原样保留并复述作者的动作词（如删除、清空、覆盖），明确说明当前只能通过 PatchSet 修改白名单字段，该动作不可用；不得把动作改写成中性词，不得生成替代性补丁
- `out_of_scope`：说明该请求与当前卷宗/工作台无关，并给出可用的能力范围
- 本组件不允许返回 `suggestions`
- 不得对不可执行动作给出“我建议修改”之类的绕过建议
