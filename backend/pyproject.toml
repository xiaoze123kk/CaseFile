[project]
name = "casefile-backend"
version = "0.1.0"
description = "CaseFile personal-product PostgreSQL foundation"
requires-python = ">=3.12,<3.13"
dependencies = [
  "alembic>=1.14,<2",
  "cryptography>=49,<50",
  "fastapi>=0.115,<1",
  "jsonschema>=4.23,<5",
  "openai-agents>=0.18,<0.19",
  "pydantic>=2.10,<3",
  "psycopg[binary]>=3.2,<4",
  "rfc8785>=0.1.4,<1",
  "sqlalchemy>=2.0.36,<3",
  "uvicorn>=0.34,<1",
]

[project.optional-dependencies]
dev = [
  "datamodel-code-generator==0.71.0",
  "httpx>=0.28,<1",
  "mypy>=1.13,<2",
  "pytest>=8.3,<10",
  "ruff>=0.8,<1",
  "types-jsonschema>=4.23,<5",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/casefile", "src/casefile_contracts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "postgres: requires CASEFILE_TEST_DATABASE_URL pointing to a disposable *_test PostgreSQL database",
  "live: opt-in real-provider acceptance; requires CASEFILE_RUN_LIVE_ACCEPTANCE=1",
]

[tool.ruff]
src = ["src", "tests"]
line-length = 100
target-version = "py312"
extend-exclude = ["src/casefile_contracts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
"src/casefile_contracts/*.py" = ["E501", "I001"]

[tool.mypy]
python_version = "3.12"
files = ["src"]
exclude = ["src/casefile_contracts/"]
strict = true
warn_unreachable = true
